{
  "$schema": "../policy.schema.json",
  "_note": "Personal skills plugin marketplace — gitflow with main as stable release, develop as integration. Checkout-based (no worktree required). Plugin marketplace clients pull main; develop accumulates unreleased changes.",
  "branchStrategy": {
    "type": "gitflow",
    "defaultBranch": "develop",
    "protectedBranches": ["main", "master", "develop"],
    "requireWorktree": false,
    "allowedPrefixes": [
      "feature/",
      "fix/",
      "chore/",
      "refactor/",
      "hotfix/",
      "release/",
      "docs/",
      "test/"
    ]
  }
}
